# Tasks

A running list of planned work and known issues for notes-to-do.

## Open

- [ ] **Editing on the preview format of Markdown files** — Allow editing
  directly in the rendered Markdown preview (WYSIWYG-style), instead of only in
  the raw source pane.

- [ ] **Make hovering on the left menu bar more visible** — Improve the hover
  state styling of the left sidebar (vault menu) so hovered items stand out more
  clearly.
